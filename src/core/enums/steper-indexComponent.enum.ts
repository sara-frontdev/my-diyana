export enum RegisterStepIndex {
  register = 0,
  verifyCode = 1,
  finalizeRegister = 2,
}

export enum ForgotPasswordStepIndex {
  forgotPassword = 0,
  resetPassword = 1,
}

export enum LoginSmsCodeStepIndex {
  PrimaryInfo = 0,
  verifyCode = 1,
}
